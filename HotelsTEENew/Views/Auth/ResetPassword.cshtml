@{
    ViewBag.Title = "Νέος Κωδικός";
    Layout = "~/Views/Shared/_AnonymousLayout.cshtml";
}

<div class="login-bg account-pages pt-2 pt-sm-5 pb-4 pb-sm-5 position-relative">
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-xxl-4 col-lg-5">
                <div class="card">
                    <div class="py-4 text-center login-info-panel">
                        <div>
                            <h3>Ορισμός Νέου Κωδικού</h3>
                            <small>@ViewBag.Email</small>
                        </div>
                    </div>

                    <div class="card-body p-4">
                        <div id="step-form">
                            <div class="mb-3">
                                <label class="form-label">Νέος κωδικός</label>
                                <div class="input-group input-group-merge">
                                    <input type="password" id="password" class="form-control" placeholder="Τουλάχιστον 7 χαρακτήρες, 1 ειδικός (π.χ. ! @@ #)" />
                                    <div class="input-group-text" data-password="false"><span class="password-eye"></span></div>
                                </div>
                            </div>
                            <div class="mb-3">
                                <label class="form-label">Επιβεβαίωση κωδικού</label>
                                <input type="password" id="password2" class="form-control" />
                            </div>

                            <div id="msg" class="alert alert-danger d-none py-2"></div>
                            <div class="text-center">
                                <button class="btn login-info-panel" id="btn-submit">Αποθήκευση κωδικού</button>
                            </div>
                        </div>

                        <div id="step-done" class="text-center" style="display:none;">
                            <i class="mdi mdi-check-circle-outline text-success" style="font-size:48px;"></i>
                            <p class="mt-2">Ο κωδικός σας άλλαξε! Μπορείτε τώρα να συνδεθείτε.</p>
                            <a href=@Url.Action("Login", "Account") class="btn login-info-panel">Σύνδεση</a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<footer class="footer footer-alt" style="color:white">
    <script>document.write(new Date().getFullYear())</script> © ΞΕΝΟΔΟΧΕΙΑΚΟ ΕΠΙΜΕΛΗΤΗΡΙΟ ΕΛΛΑΔΟΣ
</footer>

<script>
    (function () {
        var btn = document.getElementById("btn-submit"), msg = document.getElementById("msg");
        btn.addEventListener("click", function () {
            var p1 = document.getElementById("password").value;
            var p2 = document.getElementById("password2").value;
            msg.classList.add("d-none");
            if (p1 !== p2) { msg.classList.remove("d-none"); msg.textContent = "Οι κωδικοί δεν ταιριάζουν."; return; }
            btn.disabled = true; btn.textContent = "Αποθήκευση...";
            fetch('@Url.Action("ResetSubmit", "Auth")', {
                method: "POST",
                headers: { "Content-Type": "application/x-www-form-urlencoded" },
                body: "token=" + encodeURIComponent('@ViewBag.Token') + "&password=" + encodeURIComponent(p1)
            }).then(function (res) { return res.json(); }).then(function (r) {
                btn.disabled = false; btn.textContent = "Αποθήκευση κωδικού";
                if (r && r.success) {
                    document.getElementById("step-form").style.display = "none";
                    document.getElementById("step-done").style.display = "block";
                } else { msg.classList.remove("d-none"); msg.textContent = (r && r.message) || "Παρουσιάστηκε σφάλμα."; }
            }).catch(function () {
                btn.disabled = false; btn.textContent = "Αποθήκευση κωδικού";
                msg.classList.remove("d-none"); msg.textContent = "Σφάλμα επικοινωνίας.";
            });
        });
    })();
</script>
